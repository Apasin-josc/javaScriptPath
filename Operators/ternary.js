//Ternary operators
//If a customer has more than 100 pints, they are a 'gold' customer, otherwise, they are 'silver' customer.

let points = 110;
//ternary or condition operator
let type = points > 100 ? "gold" : "silver";

//if the condition of points > 100 is True, set it type to gold if not set it to silver 😋
